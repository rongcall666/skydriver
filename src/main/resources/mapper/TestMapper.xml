<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skydriver.mapper.TestMapper">

		
	<insert id="insertA">
		insert into a
		values(#{id},#{name})
	</insert>	
		
	<insert id="insertB">
		insert into b
		values(#{id},#{name})
	</insert>		
</mapper>